function removePeople(demo) {
  var mofifiedData = {
    title: demo.title,
    name: demo.name,
    mail: demo.mail,
    phone: demo.phone,
    designation: demo.designation,
    department: demo.department
  };
  // return mofifiedData;
  return mofifiedData;
}

export {removePeople}